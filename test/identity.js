// const Identity = artifacts.require('Identity.sol');

// contract('Identity', (accounts) => {
//   describe('a deployed contract', () => {
//     // let identityInstance = {};

//     before(async () => {
//       // identityInstance = await Identity.deployed();
//     });

//     it('...should have expected data',
//       async (/* done */) => {

//         // let version = await identityInstance.VERSION();
//         // assert.equal(version, '6.1.0', 'contract version is not \'6.1.0\'');

//         // let metadata = await identityInstance.Metadata();
//         // assert.equal(metadata, '0x48060d', 'metadata is not \'0x48060d\'');

//         // let encoding = await identityInstance.Encoding();
//         // assert.equal(encoding, 'application/json', 'metadata encoding is not \'application/json\'');

//         // done();

//         assert.ok(true);
//       });
//   });
// });
